<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Homepage</title>
</head>
<body class="bg-zinc-900">
    <!-- Main Container -->
    <div class="bg-zinc-900 min-h-screen w-full text-white">
        <!-- Header with Logout Button -->
      

        <!-- Content Container -->
        <div class="container mx-auto px-4">
            <form action="/update/<%= post._id %>" method="post" class="flex gap-4 mb-8 mt-8">
                <input
                    value="<%= post.content %>"
                    class="flex-grow bg-zinc-700 px-4 rounded-md focus:outline-none h-12"
                    type="text"
                    name="content"
                    placeholder="What's on your mind?"
                    required
                >
                <button
                    class="px-6 py-2 bg-green-900 text-xl font-bold rounded-md cursor-pointer hover:bg-green-800"
                    type="submit">
                    Update
                </button>
            </form>

            <hr class="border-gray-700 mb-8">

        </div>
    </div>
</body>
</html>
